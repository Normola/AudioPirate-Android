# AudioPirate - Server Management Architecture

## Component Diagram

```
┌─────────────────────────────────────────────────────────────────┐
│                         MainActivity                             │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │                    AudioPirateApp                          │  │
│  │  - Bottom Navigation (Home, Search, Downloads, Settings)  │  │
│  └───────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
                                 │
                                 ▼
┌─────────────────────────────────────────────────────────────────┐
│                         HomeScreen                               │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │  TopBar: "🏴‍☠️ AudioPirate"                                │  │
│  ├───────────────────────────────────────────────────────────┤  │
│  │  Empty State (when no servers):                           │  │
│  │    "No servers configured"                                │  │
│  │    "Tap the + button to add a WebSocket server"           │  │
│  ├───────────────────────────────────────────────────────────┤  │
│  │  OR                                                        │  │
│  ├───────────────────────────────────────────────────────────┤  │
│  │  Server List (LazyColumn):                                │  │
│  │  ┌─────────────────────────────────────────────────────┐  │  │
│  │  │ ServerCard 1                                        │  │  │
│  │  │  - Name: "Production Server"                        │  │  │
│  │  │  - URL: wss://api.example.com:8080                  │  │  │
│  │  │  - Description: "Main server"                       │  │  │
│  │  │  - [Active Switch] [⋮ Menu]                         │  │  │
│  │  └─────────────────────────────────────────────────────┘  │  │
│  │  ┌─────────────────────────────────────────────────────┐  │  │
│  │  │ ServerCard 2                                        │  │  │
│  │  │  - Name: "Development Server"                       │  │  │
│  │  │  - URL: wss://dev.example.com:8080                  │  │  │
│  │  │  - [Inactive Switch] [⋮ Menu]                       │  │  │
│  │  └─────────────────────────────────────────────────────┘  │  │
│  └───────────────────────────────────────────────────────────┘  │
│                                                                  │
│  [+] FAB (Floating Action Button) → Opens AddServerDialog       │
└─────────────────────────────────────────────────────────────────┘
                                 │
                                 │ observes servers StateFlow
                                 ▼
┌─────────────────────────────────────────────────────────────────┐
│                      ServerViewModel                             │
│  - servers: StateFlow<List<ServerEntity>>                       │
│  - addServer(name, url, description)                            │
│  - updateServer(server)                                         │
│  - deleteServer(server)                                         │
│  - toggleServerActive(server)                                   │
└─────────────────────────────────────────────────────────────────┘
                                 │
                                 │ uses
                                 ▼
┌─────────────────────────────────────────────────────────────────┐
│                     ServerRepository                             │
│  - getAllServers(): Flow<List<ServerEntity>>                    │
│  - getActiveServers(): Flow<List<ServerEntity>>                 │
│  - insertServer(server): Long                                   │
│  - updateServer(server)                                         │
│  - deleteServer(server)                                         │
└─────────────────────────────────────────────────────────────────┘
                                 │
                                 │ uses
                                 ▼
┌─────────────────────────────────────────────────────────────────┐
│                        ServerDao                                 │
│  @Query("SELECT * FROM servers ORDER BY createdAt DESC")        │
│  @Insert, @Update, @Delete operations                           │
└─────────────────────────────────────────────────────────────────┘
                                 │
                                 │ accesses
                                 ▼
┌─────────────────────────────────────────────────────────────────┐
│                       AppDatabase                                │
│  Room Database (SQLite)                                         │
│  - Table: servers                                               │
└─────────────────────────────────────────────────────────────────┘
                                 │
                                 │ persists
                                 ▼
┌─────────────────────────────────────────────────────────────────┐
│                     ServerEntity (Table)                         │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │ id (PK)      │ name          │ url                        │  │
│  │ description  │ isActive      │ createdAt                  │  │
│  ├───────────────────────────────────────────────────────────┤  │
│  │ 1           │ Prod Server    │ wss://api.example.com:8080│  │
│  │ 2           │ Dev Server     │ wss://dev.example.com:8080│  │
│  └───────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
```

## Data Flow

### Adding a Server
```
User clicks FAB
    │
    ▼
AddServerDialog opens
    │
    ▼
User enters: name, URL, description
    │
    ▼
Validation checks (name not blank, URL format)
    │
    ▼
User clicks "Add"
    │
    ▼
viewModel.addServer(name, url, description)
    │
    ▼
repository.insertServer(ServerEntity(...))
    │
    ▼
serverDao.insertServer(server)
    │
    ▼
Room Database saves to SQLite
    │
    ▼
DAO emits updated Flow<List<ServerEntity>>
    │
    ▼
Repository Flow updates
    │
    ▼
ViewModel StateFlow updates
    │
    ▼
HomeScreen recomposes with new server in list
```

### Toggling Server Active State
```
User toggles switch on ServerCard
    │
    ▼
onToggleActive callback
    │
    ▼
viewModel.toggleServerActive(server)
    │
    ▼
repository.updateServer(server.copy(isActive = !server.isActive))
    │
    ▼
serverDao.updateServer(server)
    │
    ▼
Room Database updates record
    │
    ▼
DAO emits updated Flow
    │
    ▼
UI recomposes with updated state
```

## Key Features

### 1. **Reactive UI**
- Uses Kotlin Flow and StateFlow
- UI automatically updates when database changes
- No manual refresh needed

### 2. **Clean Architecture**
- Separation of concerns (UI, ViewModel, Repository, DAO, Database)
- Each layer has a single responsibility
- Easy to test and maintain

### 3. **User-Friendly**
- Empty state guides users
- Validation prevents errors
- Immediate visual feedback
- Simple, intuitive interface

### 4. **Persistent Storage**
- Room Database (SQLite)
- Data survives app restarts
- Efficient queries with indexing

### 5. **Material Design 3**
- Modern UI components
- Consistent styling
- Follows Android design guidelines
